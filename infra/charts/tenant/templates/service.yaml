apiVersion: v1
kind: Service
metadata:
  name: odoo
  labels:
    app: odoo
spec:
  selector:
    app: odoo
  ports:
    - name: web
      port: 8069
      targetPort: web
    - name: websocket
      port: 8072
      targetPort: websocket
